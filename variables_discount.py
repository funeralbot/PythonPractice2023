#Variables are names for values
x = 15 #python is creating the vaule after the X

price = 9.99 #the data type stored in the variable is a float because of the decimal point.

discount = 0.2 #the discount amount

result = price * (1 - discount) #the math logic used to get the final price after the discount is applied.

print(result) #display the final price to the user